node fibo() returns (x : int);
let
    x_1 = 1 fby x;
    x_0 = 1 fby x_1; 
    x = x_0 + x_1; 
tel

#[test]
node verify_5_first_value() returns (z: bool);
let
    z = fibo([(), (), (), (), ()]) == [2, 3, 5, 8, 13];        
tel
