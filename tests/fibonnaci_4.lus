node fibo() returns (z : int);
let
	x0 = pre z;
	x1 = pre pre z;
	add = x0 + x1;
	z = 1 -> (1 -> add);
tel

#[test]
node test2() returns (z : bool);
let
	lhs = fibo([(), (), (), ()]);
	rhs = [2, 4, 6];
	z = lhs == rhs;
tel
